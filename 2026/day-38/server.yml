server:
  server name: Appserver-01
  ip adress: 192.168.0.12
  port: 8080
database:
  host: db.exampl.com
  name: app_database
  credential:
    user: db_admin
    password: securep@ssword
startup_script_preserve: |
  #!bin/bash
  echo "starting server.."
  sytemctl start nginx
  echo "nginx started sucessfully"
